import FilterOption from "./FilterOption";

export default interface NameFilters {
  genderOptions: FilterOption[];
  decadeOptions: FilterOption[];
  cultureOptions: FilterOption[];
  languageOptions: FilterOption[];
}
